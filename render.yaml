services:
  - type: web
    name: atmos-lab
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: HF_TOKEN
        sync: false   # secret — paste your Hugging Face token in the Render dashboard
